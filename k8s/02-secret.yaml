apiVersion: v1
kind: Secret
metadata:
  name: pos-integration-secret
  namespace: pos-integration
type: Opaque
# Values are base64-encoded.
# Generate with: echo -n 'your_value' | base64
# IMPORTANT: Replace these placeholder values before deploying.
data:
  DB_USERNAME: cm9vdA==          # root
  DB_PASSWORD: cGFzc3dvcmQ=     # password  ← change in production!
  MYSQL_ROOT_PASSWORD: cGFzc3dvcmQ=
